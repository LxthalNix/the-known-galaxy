import type { EraId } from './eras';
/** Optional future chapter art. Use a supplied local asset; no art is needed for either concept. */
export const experimentalEraArt: Partial<Record<EraId, string>> = {};
